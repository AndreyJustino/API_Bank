import { Router } from "express";

const routerUser = Router()

routerUser.get("/", () => {})
routerUser.post("/", () => {})
routerUser.delete("/", () => {})
routerUser.put("/", () => {})

export default routerUser