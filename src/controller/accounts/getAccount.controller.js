import { accounts } from "../../model/model.jsx";

async function getAccounts(req,res) {
    try{

    }catch(error){
        console.error("Error in getAccount: ",error.message);
    }
}

export default getAccounts