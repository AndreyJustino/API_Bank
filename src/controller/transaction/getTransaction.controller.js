import { transactions } from "../../model/model.js";

async function getTransaction(res,req){
    try{

    }catch(error){
        console.log("Error in getTransaction:",error.message);
    }
}

export default getTransaction